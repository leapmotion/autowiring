// Copyright (c) 2010 - 2013 Leap Motion. All rights reserved. Proprietary and confidential.
#include "stdafx.h"
#include "StateMachine.h"

/*
template<typename T_enum>
StateMachine<>::StateMachine(T_enum startState)
{
  m_currentState = startState;
}

StateMachine::~StateMachine()
{

}

template<typename T_enum, typename T_input>
T_enum StateMachine<T_enum>::transition(T_input input){
  m_currentState = m_transitionMap[m_currentState];
  return m_currentState;
}

template<typename T_enum, typename T_input>
T_enum StateMachine<T_enum>::addTransition(T_enum state, lambda){
  m_transitionMap[state] = lambda;
}

template<typename T_enum>
T_enum StateMachine::getState(){
  return m_currentState;
}
*/